# Momentum queues (live)

Update in-place during work. Never end a substantial run with all sections empty.

**Last updated:** 2026-04-11

---

## now

- Bootstrap agent-os-program file pack + smoke script (this session).

## next

- Run `scripts/agent-os-milestone1-smoke.sh` from repo root; fix any failures.
- Complete `verification/MILESTONE_1_CHECKLIST.md` for one real company + task path.
- Append first row to `METRICS_LOG.md`.

## blocked

- *(none — add company_id / API token / DB URL here when smoke is blocked)*

## improve

- Wire **one** field from `HsmRunnerConfig` promotion story into a **live** runtime doc or feature flag spec (per `EVAL_AND_META_HARNESS.md` gap).
- Add verifier JSON schema stub for “task done” beyond agent self-assertion.

## recurring

- Weekly: skim `docs/agent-os-program/FAILURE.md` and open eval smoke (`hsm-eval --limit 2`) when LLM available.
